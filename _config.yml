# Site settings
title: IEEE SciVis Contest
email: web@ieeevis.org
description: > # this means to ignore newlines until "baseurl:"
  Website for IEEE SciVis Contest
baseurl: "" # the subpath of your site, e.g. /blog/
url: "http://sciviscontest.ieeevis.org" # the base hostname & protocol for your site

# Build settings
markdown: kramdown

exclude: ['README.md', 'Makefile', 'scripts', '.DS_Store']

defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      contact: "web@ieeevis.org"
